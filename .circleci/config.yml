version: 2
jobs:
  build:
    docker:
      - image: circleci/clojure:lein-2.7.1

    working_directory: ~/repo

    environment:
      LEIN_ROOT: "true"

    steps:
      - checkout
      - run: ./bin/lein-all.sh do clean, check, midje, install
